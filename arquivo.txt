sudo apt install git

# consfiguração inicial do git
git init
 git config --global user.name"Talmiro"
 git config --global user.email "talmiro@sostrader.com.br"
 
 # Editei o projeto 
 git add .
 git commit -m "A mensagem"
 git push origin master
 
